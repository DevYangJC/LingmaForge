# 2026-06-26 工作日志

## AI 代码生成核心功能架构方案

### 完成内容
- 输出完整架构方案文档: `outputs/灵码工坊-AI代码生成核心功能架构方案.md`
- 涵盖九大章节: 核心功能全景、前端组件规划、后端技术架构、AI框架选型与生成引擎设计、核心数据流设计、API接口规范、数据库设计补充、沙箱运行环境设计、开发路线与优先级

### 关键技术决策
- AI框架: Spring AI Alibaba 统一方案 (AI接入+AI编排一个框架完成)
- LLM模型: Claude 3.5 Sonnet (代码生成主力) + DeepSeek-V3 (需求分析/辅助) + Qwen (DashScope)
- 流式推送: SSE (生成进度) + WebSocket (沙箱日志)
- 编辑器: Monaco Editor
- 沙箱: Docker容器 + Nginx反向代理
- 前端新增依赖: monaco-editor, markdown-it, splitpanes, xterm, axios

### AI 框架技术选型修订
- 完成深度对比文档: `outputs/灵码工坊-AI框架技术选型深度对比.md`
- 对比四个方案: SAA统一方案、SAA+LangGraph4j、SAA+LangChain4j、SAI+LangGraph4j
- 最终推荐: Spring AI Alibaba 统一方案（接入+编排全在一个框架）
- 核心理由: 国产模型一等公民、Graph编排已成熟、内置ReactAgent减少开发量、统一依赖树零版本冲突
- 预留降级策略: 接口抽象层，可通过配置切换到LangGraph4j

### 前端核心组件规划
- ChatPanel.vue (AI对话 + SSE流式接收)
- FileTreePanel.vue (动态文件树)
- EditorPanel.vue (Monaco Editor + Diff模式)
- PreviewPanel.vue (iframe沙箱预览)
- LogPanel.vue (WebSocket日志)
- WorkbenchToolbar.vue (项目操作工具栏)
- ProjectSidebar.vue (项目列表)

### 后端核心模块
- GenerationController + SseEmitter (流式推送)
- GenerationPipeline (六阶段流水线: 需求分析→执行规划→代码生成→样式优化→构建验证→预览部署)
- 6个Agent: RequirementAnalysis / ExecutionPlanning / CodeGeneration / ApiGeneration / StyleOptimization / Iteration
- SandboxService (Docker容器调度)

### 架构方案 v2.0 重写（使用 Context7 实时文档）
- 基于 Spring AI Alibaba 1.1.2 全面重写后端架构：
  - 完整 Maven pom.xml 依赖配置（含 spring-ai-bom 1.1.2 + spring-ai-alibaba-bom 1.1.2.2）
  - application.yml 多模型配置（DashScope / DeepSeek 兼容模式 / OpenAI 兼容）
  - StateGraph 六阶段流水线代码（含条件回退边）
  - ReactAgent 节点实现（需求分析 + 代码生成 + 工具定义）
  - SSE 流式推送（DataAgent 模式：Sinks.Many.unicast + Flux<ServerSentEvent>）
  - Redis Checkpoint 持久化 + 断点恢复
  - 5 个 Mermaid 图：系统架构、StateGraph 状态机、完整数据流时序、数据库 ER、甘特图
- 文档路径: outputs/灵码工坊-AI代码生成核心功能架构方案.md (v2.0)

### 架构方案 v3.0 重排版（教学项目首篇文章）
- 全面重排版，定位为教学项目第一篇文章
- 新增大段描述性内容：
  - "灵码工坊是什么？"——用大白话解释产品定位
  - "用户想要什么体验？"——完整的用户体验 walkthrough
  - "为什么第一步就做核心组件？"——三层论证（产品价值验证、技术风险、比喻）
  - 每个技术章节都有"大白话先讲清楚，再进技术细节"的叙事结构
- 11 个章节重新编排，渐进式深入
- 5 个 Mermaid 图保留并增加了叙事性注释
- 文档路径: outputs/灵码工坊-AI代码生成核心功能架构方案.md (v3.0)

### 后端核心功能实现指南 v2.0（采用 .entity() 结构化输出）
- 更新深度实现文档: `outputs/灵码工坊-后端核心功能实现指南.md`
- 核心变更：采用 Spring AI `.entity()` 方法替代手动 JSON 解析
  - 删除 JsonOutputParser 容错类和 validateJson ToolCallback
  - 删除"只输出JSON"提示词约束（.entity() 自动约束格式）
  - 格式定义单一来源改为 Java 类，不再维护提示词和 Java 类两份格式定义
  - State 存储从 JSON 字符串改为直接存 Java 对象（类型安全）
  - ToolCallback 从 7 个减少到 6 个
  - 新增"结构化输出的正确方式"章节，详述 .entity() 四种用法
  - 需求分析和执行规划节点实现代码全面更新
  - GenerationStateAccessor 简化（不再需要 JSON.parseObject 中间态）
