# =============================================================================
# 灵码工坊 (LingmaForge) 后端配置入口
# =============================================================================
# 本文件仅做两件事：
#   1. 指定默认 profile 为 dev
#   2. 所有实际配置在 application-dev.yml 中定义
# =============================================================================

spring:
  profiles:
    active: local
